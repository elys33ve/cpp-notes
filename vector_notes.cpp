// this says cpp file but it actually just notes on vector stuff

#include <vector>

vector<int> v;

v.size()
v.resize()
v.at()

v.push_back()               //append
v.back()                    //last element
v.pop_back()                //remove last element   

for (i = v.size() - 1; i>=0; i--){  

strcpy(x,y)                 //copies y to x
strncpy(x,y,numchars)
strcat(x,y)                 //copies to end
strncat(x,y,numchars)

strchr(y,searchchar)
strlen(y)
strcmp(str1,str2)           //return 0 if strs are equal
